-- SELECT * FROM [Project].Manufactures -- effected addAssemply Q4, add process ID to end
-- SELECT * FROM [Project].[Records]    -- effected addTransaction Q8 (add jobNo to end)
-- Rerun java code and screenshot:
    -- 10
    -- 11
    -- 15

-- Change in java and make sure still works
-- Run procedure for each table to make it up to date
-- Change both final screenshots to it looks right